#include<stdio.h>
int main()
{
    int count=1,i;
    while(count>0)
    {
        printf("Enter number ");
        scanf("%d",&i);
        if(i==0)
        {
            break;
        }
    }
}